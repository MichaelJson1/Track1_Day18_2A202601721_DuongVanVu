# Three-Option Design Sheet — AI Notes: Personal Learning Notes

**Case:** Case B — AI Notes: Personal Learning Notes
**Hypothesis Problem:** Khi có việc buộc phải dùng lại nội dung của một bài đã học cách đó vài ngày, học viên có thể gặp khó khăn trong việc lấy lại đúng phần mình cần vì nội dung nằm rải ở nhiều nơi và không có điểm vào rõ ràng; điều này có thể khiến họ phải tua và đọc lại nhiều hơn dự tính, hoặc bỏ cuộc giữa chừng và làm tiếp việc đang dở với hiểu biết mơ hồ.

## 1. Ba option (nhắc lại từ Group Feedback Synthesis)

| Thành phần | Option A | Option B | Option C |
|---|---|---|---|
| Solution mechanism | Mẫu ghi chú có cấu trúc sẵn để học viên tự kéo/thả highlight, điểm chưa hiểu, câu hỏi vào đúng mục — không có bước AI xử lý nội dung | AI đọc highlight/điểm chưa hiểu/câu hỏi, gợi ý nhóm nội dung theo chủ đề và đề xuất phần nên đưa vào ghi chú; học viên chọn, sửa, bổ sung trước khi lưu | AI tự động tổng hợp toàn bộ highlight/điểm chưa hiểu/câu hỏi thành một bản ghi chú hoàn chỉnh ngay khi kết thúc bài; học viên chỉ xem lại và xác nhận |
| Vị trí trên spectrum | USER CREATES / INITIATES | USER + AI CO-CREATE | AI CREATES / INITIATES, USER REVIEWS |

## 2. Human–AI Decision Table

| Human–AI decision | Option A | Option B | Option C |
|---|---|---|---|
| **User làm gì? AI làm gì?** | User tự kéo/thả, phân loại và viết lại nội dung vào mẫu có sẵn (mục "Ý chính", "Chưa hiểu", "Câu hỏi"...). AI không tham gia xử lý nội dung, chỉ cung cấp khung mẫu. | AI đọc highlight/điểm "Chưa hiểu"/câu hỏi, nhóm theo chủ đề và đề xuất phần nên đưa vào ghi chú. User chọn/bỏ từng gợi ý, chỉnh sửa câu chữ, bổ sung phần AI bỏ sót, rồi xác nhận lưu. | AI tự chọn lọc, nhóm và viết thành một bản ghi chú hoàn chỉnh ngay sau khi kết thúc bài, tự flag đoạn không chắc chắn. User đọc lại bản nháp, sửa chỗ sai/thiếu, xác nhận lưu hoặc yêu cầu AI tạo lại. |
| **AI Act / Ask / Don't Act? Vì sao?** | **Don't Act** — AI hoàn toàn không can thiệp nội dung. Đây là nhánh kiểm chứng "không AI" trên spectrum; rủi ro sai chỉ đến từ chính user nên AI không cần hành động. | **Ask** — AI đưa gợi ý (nhóm chủ đề, phần nên đưa vào) nhưng dừng lại chờ user chọn/sửa trước khi bất kỳ nội dung nào vào bản ghi chú cuối. Phù hợp vì đây là gợi ý hỗ trợ quyết định, không phải quyết định thay, trong khi độ tin cậy của AI khi đọc đúng ý học viên (đặc biệt điểm "Chưa hiểu") chưa được kiểm chứng. | **Act, nhưng có điều kiện (flag khi không chắc)** — AI tạo bản nháp hoàn chỉnh ngay để tối đa tốc độ, đúng mục tiêu "giảm công sức nhiều nhất" của Option C; nhưng bắt buộc flag rõ những đoạn không chắc thay vì Act âm thầm, vì hậu quả nếu sai (học viên tin nhầm một bản ghi chú sai) là nghiêm trọng nhất trong ba option. |
| **User hiểu capability/limit bằng gì?** | Không cần giải thích capability AI vì AI không tham gia. User chỉ cần thấy các mục có sẵn trong mẫu — tự giải thích qua chính giao diện, không cần onboarding riêng. | Một dòng giới thiệu ngắn khi mở gợi ý lần đầu (vd: "AI gợi ý nhóm nội dung dựa trên highlight và ghi chú của bạn — bạn chọn phần muốn giữ"), kèm nhãn "Gợi ý" gắn cố định trên từng khối để user luôn phân biệt được gợi ý AI với nội dung gốc của mình. | Cần nói rõ nhất trong ba option vì user ít chủ động kiểm tra nhất: thông báo ngắn trước khi AI chạy (vd: "AI đang tạo bản nháp từ ghi chú của bạn — hãy xem lại trước khi lưu"), và giới hạn cụ thể (AI có thể hiểu sai phần "Chưa hiểu" nếu thiếu ngữ cảnh) được nhắc lại ngay tại chỗ AI flag, không chỉ ở màn hình giới thiệu ban đầu. |
| **Evidence/uncertainty được thể hiện thế nào?** | Không áp dụng — không có suy luận AI nào cần hiển thị evidence. | Mỗi gợi ý nhóm chủ đề hiển thị kèm trích đoạn highlight/câu hỏi gốc mà nó dựa vào, để user đối chiếu nhanh mà không cần lục lại toàn bài. Nếu AI ghép một điểm "Chưa hiểu" vào một nhóm, gắn nhãn nhẹ kiểu "chưa chắc chắn" ngay tại gợi ý đó. | Trong bản nháp, đoạn AI tự suy luận/gộp thông tin từ nhiều nguồn có dấu hiệu trực quan riêng (vd highlight màu hoặc icon) khác với đoạn AI chỉ sao chép nguyên văn. Các đoạn AI không chắc (thiếu ngữ cảnh) được flag rõ ràng ngay trong bản nháp — đúng cơ chế đã chốt ở Chặng 2 — kèm lý do ngắn (vd: "điểm Chưa hiểu này không có ghi chú đi kèm"). |
| **User kiểm soát và recovery thế nào?** | User có toàn quyền vì tự viết từ đầu. Recovery chỉ là sửa trực tiếp trong mẫu — không cần undo AI riêng, vì rủi ro sai duy nhất đến từ chính user, không phải AI. | User chọn/bỏ từng gợi ý riêng lẻ trước khi chúng vào bản ghi chú (preview có sẵn), sửa trực tiếp nội dung gợi ý đã chọn, hoặc bỏ qua toàn bộ gợi ý và viết tay như Option A. Vì chưa gì được lưu tới khi user xác nhận, recovery nằm ngay ở bước chọn — không cần undo sau lưu. | User bắt buộc xem bản nháp đầy đủ trước khi lưu (không tự lưu ngầm), có thể sửa trực tiếp từng đoạn, xoá đoạn sai, hoặc bấm "Tạo lại" để AI chạy lại từ đầu nếu bản nháp lệch nhiều. Sau khi lưu, hệ thống vẫn giữ bản gốc (highlight/ghi chú thô) để user đối chiếu nếu phát hiện sai sau này — đường quay lại việc đang dở là sửa tại chỗ trong bản nháp, không phải học lại từ đầu. |

## 3. Feedback and data check — ghi chú ngắn

Cả ba option đều dùng dữ liệu cá nhân của chính học viên trong một phiên (highlight, điểm "Chưa hiểu", câu hỏi/ghi chú của bài vừa học) và không có cơ chế học từ feedback across sessions hay across user trong phạm vi ba option này — nên mục này chưa áp dụng đầy đủ. Điểm cần giữ lại cho vòng sau: nếu về sau bổ sung cơ chế "AI học từ lần sửa của user để gợi ý tốt hơn lần sau" (đặc biệt ở Option B/C), cần nói rõ với user rằng sửa của họ có được ghi nhớ cho các bài học sau hay không, và cho phép họ rút lại việc đó.

## 4. Nhận xét ngắn

Agency tăng dần từ A → C đúng theo mức hậu quả nếu sai: Option A không có gì để mất vì AI không tham gia; Option B giới hạn thiệt hại bằng cách giữ AI ở mức "Ask" trước khi bất kỳ nội dung nào được chấp nhận; Option C chấp nhận rủi ro cao nhất (AI tự viết bản hoàn chỉnh) để đổi lấy tốc độ, nên đường kiểm soát và flag uncertainty ở Option C cần chắc chắn nhất trong ba option — đây cũng là chỗ nên ưu tiên test đầu tiên nếu phải chọn một tương tác để kiểm tra trước.
